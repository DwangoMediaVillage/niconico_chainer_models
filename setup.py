from setuptools import setup

setup(name='niconico_chainer_models',
      version='0.0.2',
      description='Model Definitions',
      author='dwango',
      author_email='',
      url='',
      packages=['niconico_chainer_models'],
      install_requires=open('requirements.txt').read().splitlines(),
      entry_points=""
)
